@model IEnumerable<PassGeneralsModels.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div>
    <br />
    <h4>Admin's Page: Registered Customers</h4>
    <br />


   
    
    @using (Html.BeginForm())
    {
        <p> First name: @Html.TextBox("SearchString") 
        <input type="submit" value="Filter" /> </p>
    }
    <br />
            @*@Html.HiddenFor(model => model.ApplicationUserId)*@

    <table class="table">
        <tr>
            <th>
                @Html.ActionLink("First name", "Index", new { sortOrder = ViewBag.NameSortParm })
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerLastname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerPhone)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CustomerFirstname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CustomerLastname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CustomerAddress)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CustomerPhone)
                </td>
                <td>
                    @Html.ActionLink("Edit", "EditForAdmin", new { id = item.CustomerId }) |
                    @Html.ActionLink("Details", "DetailsForAdmin", new { id = item.CustomerId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.CustomerId })
                </td>
            </tr>
        }

    </table>
</div>




<p>
    @*@Html.ActionLink("Edit", "Edit", new { id = Model.CustomerId }) |*@
    @*@Html.ActionLink("Back to List", "Index", "Customer")*@
    @Html.ActionLink("Create New Customer", "Create") |

</p>
